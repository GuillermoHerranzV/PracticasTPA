package entity;

import main.Controles;
import main.PanelDeJuego;

/**
 * Clase Character que hereda de Player ya que el jugador podra escoger entre distintos personajes
 */
/*public class Character extends Player {
    private int mana;
    
    public Character (PanelDeJuego gp, Controles key, int a) {
    	if(a == 1) {
    		Character.setMana(100);
    		Character.setHp(400);
    		Character.setDmg(35);
    	}
    	
    	else if(a == 2) {
    		Character.setMana(100);
    		Character.setHp(250);
    		Character.setDmg(70);
    	}
    	
    	else if(a == 3) {
    		Character.setMana(100);
    		Character.setHp(300);
    		Character.setDmg(45);
    	}
    }
    
    public int getMana() {
        return mana;
    }
    
    public void setMana(int mana) {
        this.mana = mana;
    }
    
    public void marmolSpecialAttack(Character a) {
    	a.setHp(400);
    	a.setMana((a.getMana() - 75));
    }
    
    public void caballeroSpecialAttack(Character a) {
    	a.setDmg(a.getDmg * 2);
    	a.setMana((a.getMana() - 75));
    }
    
    public void guerreroSpecialAttack(Character a) {
    	if(a.getHp <= (300 * 0.25)) {
    		a.setHp(300);
    	}
    	else {
    		a.setDmg(a.getDmg * 2);
    	}
    	a.setMana((a.getMana() - 75));
    }
    
}*/
